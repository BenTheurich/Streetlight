# Copy to deploy/.env.local on gb-dev and set permissions to 600.
# Production values only. Never copy the local development environment.
WORKOS_CLIENT_ID=
WORKOS_API_KEY=
WORKOS_COOKIE_PASSWORD=
GOOGLE_MAPS_BROWSER_API_KEY=
GOOGLE_MAPS_SERVER_API_KEY=
